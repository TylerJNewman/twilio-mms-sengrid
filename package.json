{
  "name": "example",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^8.2.0",
    "express": "^4.16.3",
    "got": "^6.7.1",
    "request": "^2.87.0",
    "twilio-run": "^2.6.0"
  },
  "devDependencies": {}
}
